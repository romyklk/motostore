{% set markPicture = asset('uploads/ads/' ~ mark.picture) %}

<div class="flex items-center justify-center py-2 px-2 bg-white rounded shadow-lg">
	<a href="{{path('app_mark_show',{'slug':mark.name})}}">
<img src="{{markPicture | imagine_filter('mini_thumb')}}" alt="{{mark.name}}" class="w-full h-16 object-contain" style="object-position: center;aspect-ratio: 16/9;" loading="lazy">



	</a>
</div>

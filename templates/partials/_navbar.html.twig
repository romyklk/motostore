<div class="bg-slate-100 px-2 py-3 sm:px-0 {# sticky top-0 z-100 #} shadow-lg">

	<div class="container mx-auto ">
		<div class="flex items-center justify-arround">
			<div class="w-1/2 px-5">
				<a href="{{path('app_home')}}" class="">
					<img src="{{asset('uploads/' ~ siteSettings.siteLogo)}}" alt="logo" style="width: 50px; height: 50px;">
				</a>
			</div>
			<div class="w-1/2 text-right">
				<a href="{{path('app_ad')}}" class="text-slate-500 hover:text-[#f75842] px-3 py-2 rounded-md text-sm font-bold">Nos offres</a>
				<a href="{{path('app_service')}}" class="text-slate-500 hover:text-[#f75842] px-3 py-2 rounded-md text-sm font-bold">Services</a>

				<a href="{{path('app_contact')}}" class="text-slate-500 hover:text-[#f75842] px-3 py-2 rounded-md text-sm font-bold">Contact</a>

			</div>
		</div>
	</div>
</div>
